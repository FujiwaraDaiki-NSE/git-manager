[project]
name = "gitdash-backend"
version = "1.0.0"
description = "PC内のGitリポジトリを登録なしで一覧する読み取り専用API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "inotify-simple>=1.3.5",
]

[tool.pytest.ini_options]
pythonpath = ["."]
